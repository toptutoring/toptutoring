<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="widget">
      <div class="widget-heading">
        <h3 class="widget-title text-center">Invoice session</h3>
      </div>
      <div class="widget-body">
        <%= form_for(@invoice) do |f| %>

          <div class="form-group">
            <%= f.label :student %>
            <span class="form-info"><%= @student.student.name %></span>
          </div>

          <div class="form-group">
            <%= f.label :subject %>
            <span class="form-info"><%= @student.assignment.subject %></span>
          </div>

          <div class="form-group">
            <%= f.label :hours %>
            <%= f.number_field :hours, class:"form-control", required: true, min:"0.5", pattern: "[0-9]", step: "0.5"%>
          </div>

          <div class="form-group">
            <%= f.label :description %>
            <%= f.text_field :description, class:"form-control" %>
          </div>

          <%= f.hidden_field :student_id, value: @student.id %>

          <br />
          <div class="text-center">
            <button type="submit" class="btn btn-outline btn-success">Submit</button>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
