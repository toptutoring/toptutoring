<%=# render "dwolla_login" if !current_user.has_valid_dwolla? %>
<!-- Don't show Dwolla since we are migrating to Stripe -->
<%= render "bank_linking_authentication" if current_user.stripe_uid.blank?%>

<%= render "engagements" %>

<div class="row">
  <div class="col-md-offset-3 col-md-6">
    <%= render partial: "invoice_form" %>
  </div>
</div>
