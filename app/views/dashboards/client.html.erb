<%= render "menu" %>
<% unless current_user.enabled? %>
  <%= render "users/set_up_account" %>
<% end %>
<% if current_user.enabled? %>
  <% if !current_user.is_student? %>
    <% if !any_engagements_active?(@engagements) %>
      <%= render "student_pending_engagement_message" %>
    <% else %>
      <%= render "student_active_engagement_message" %>
    <% end %>
  <% else %>
    <% if @engagements.last.pending? %>
      <%= render "pending_engagement_message" %>
    <% else %>
      <%= render "active_engagement_message" %>
    <% end %>
  <% end %>
<% end %>
