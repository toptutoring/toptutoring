<div class="col-md-12 set-up-account">
  <div class="widget">
    <div class="widget-heading">
      <h4>Thanks for using Top Tutoring! We provide excellent online tutoring
      to people all over the world.</h4>
      <p>
        Just a couple more things and your account will be ready to use.
      </p>
    </div>
    <div class="widget-body">
      <%= form_for(current_user, url: user_path(current_user), html: { method: :put, id: "form-vertical" }) do |f| %>
        <h3>Account Information</h3>
        <fieldset>
          <div class="row">
            <div class="col-md-6">
              <div class="form-group">
                <h5>Phone Number</h5>
                <%= f.text_field number_to_phone(:phone_number), value: number_to_phone(current_user.phone_number), class:"form-control", placeholder: "(XXX) XXX-XXXX"%>
              </div>
            </div>
            <div class="col-md-4">
              <div class="widget">
                <div class="widget-heading clearfix" style="background-color: #ffffee;">
                  <p>Phone numbers are completely secure with us and we use them to provide a better experience for you.</p>
                </div>
              </div>
            </div>
          </div>
        </fieldset>
        <% unless current_user.is_student? %>
          <%= render "users/student_fields", f: f %>
        <% end %>
        <%= f.submit "", class:"hidden-submit" %>
      <% end %>
    </div>
  </div>
</div>
