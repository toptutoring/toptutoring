<% unless @user.archived? %>
    var userRow = document.getElementById("row_user_<%= @user.id %>");
userTable = userRow.parentNode;
userTable.removeChild(userRow);

var rowPrevious = document.getElementById("row_user_<%= @previous_user_id %>");
rowPrevious.insertAdjacentHTML("afterend", "<%= j render @user %>")
<% end %>

  renderTopTutoringFlashes("<%= j render "application/flashes" %>");
window.scrollTo(0, 0)
