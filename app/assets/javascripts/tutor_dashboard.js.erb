$(document).ready(function(){
  $('#invoice_student_id').click(function() { 
    tutor_type = $(this).find(":selected").data("tutor-type");
    $('.invoice-tutor-type').text(tutor_type);    
  })
});
